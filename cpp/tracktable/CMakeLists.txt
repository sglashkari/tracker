cmake_minimum_required(VERSION 2.8)
project( tracktable )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( tracktable tracktable.cpp )
target_link_libraries( tracktable ${OpenCV_LIBS} )
